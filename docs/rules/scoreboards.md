# Scoreboards

## Public Scoreboards

Public scoreboard objective names **MUST** be in the form `<pack ID>.<name>`, where `name` **MUST** conform to the following:
* Contain only alphanumeric characters, `_`, and `-`